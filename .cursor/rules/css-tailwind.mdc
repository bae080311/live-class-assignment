---
description: Rules for Tailwind config and globals.css — design tokens, no arbitrary values
globs: "{tailwind.config.ts,app/globals.css}"
alwaysApply: false
---

# CSS/Tailwind 규칙 — tailwind.config.ts + app/globals.css

## 필수 사항

1. 새 색상/간격은 `tailwind.config.ts`의 `theme.extend`에 추가 — 임의 값(`[...]`) 최소화
2. 디자인 토큰은 config에 정의된 것 사용
3. `globals.css`는 `@tailwind` 지시문 + 전역 베이스 스타일만 유지

## globals.css 구조

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css');

@layer base {
  body {
    @apply font-sans text-ink-1 bg-[#e9ebef];
  }
}
```

## tailwind.config.ts 토큰

| 카테고리 | 클래스 예시 | 용도 |
|---|---|---|
| `colors.brand` | `bg-brand`, `text-brand` | 주요 액션 |
| `colors.danger` | `text-danger`, `bg-danger-soft` | 에러 |
| `colors.success` | `text-success` | 성공 |
| `colors.ink.1~5` | `text-ink-1` ~ `text-ink-5` | 텍스트 회색조 |
| `borderRadius` | `rounded-sm` ~ `rounded-xl` | 테두리 반지름 |
| `fontFamily.sans` | 자동 적용 | Pretendard |

## 새 토큰 추가

```typescript
// tailwind.config.ts
theme: {
  extend: {
    colors: {
      'new-color': 'oklch(...)',
    }
  }
}
```

## 안티패턴

- ❌ 임의 값 남발 (`text-[#333]` → `text-ink-1`)
- ❌ 컴포넌트 스타일을 `globals.css`에 작성
- ❌ `!important` 사용
- ❌ `tailwind.config.ts` 외부에 새 디자인 토큰 하드코딩
